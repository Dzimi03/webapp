# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here

# Ticketmaster API (opcjonalne)
NEXT_PUBLIC_TICKETMASTER_API_KEY=your-ticketmaster-api-key

# Redis (produkcyjna persystencja danych)
# Uzyskaj z Upstash (REST URL i TOKEN) i ustaw w Vercel / lokalnie
UPSTASH_REDIS_REST_URL=your-upstash-redis-rest-url
UPSTASH_REDIS_REST_TOKEN=your-upstash-redis-rest-token

# Vercel Blob (przechowywanie avatarów)
# Na Vercel NIE MUSISZ ustawiać tokena, jeśli projekt jest połączony ze store Blob.
# Ten token jest przydatny lokalnie, aby móc zapisywać do Blob podczas dev.
BLOB_READ_WRITE_TOKEN=your-vercel-blob-read-write-token
